package hello;

import javax.swing.JFrame;

// The program creates a window that has four fields. The fields are a blank text field, a text field with text, an uneditable text field,
// and a password text field. When the user presses enter on each field, information from each field shows up.


public class apples {
	public static void main(String[] args) {
	
	tuna bucky = new tuna();
	bucky.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	bucky.setSize(350, 100);
	bucky.setVisible(true);
	
	}
}
