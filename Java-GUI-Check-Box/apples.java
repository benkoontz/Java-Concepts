package halo;

import javax.swing.JFrame;

// A user types into a text field.
// The user can make the text in the text field change by clicking either the bold check box or the italic check box.

public class apples {
	public static void main(String[] args) {
	
	Gui bucky = new Gui();
	bucky.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	bucky.setSize(300, 200);
	bucky.setVisible(true);
	
	}
}
