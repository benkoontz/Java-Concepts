package hello;

import javax.swing.JFrame; // gives access to using GUI

// The program creates a GUI with a sentence. When the user hovers over the sentence a message shows up.

public class apples {
	
	public static void main(String[] args) {
		
		tuna bucky = new tuna();
		bucky.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // program terminates on x button
		bucky.setSize(275,180);
		bucky.setVisible(true);
		
		
	}

}
